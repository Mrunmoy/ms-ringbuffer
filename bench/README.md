# Benchmarks and Footprint Reports

This folder contains:
- **Runtime benchmarks** (Google Benchmark)
- **Footprint reports** (binary code size + RAM segments + template `sizeof`)

## Build

Benchmarks are only built in standalone mode.

```bash
python3 build.py -b